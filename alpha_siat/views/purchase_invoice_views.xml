<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_move_form_bo_purchase_info" model="ir.ui.view">
        <field name="name">account.move.form.bo.purchase.info</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <xpath expr="//notebook" position="inside">
                <page id="bo_information"
                      string="BO Information"
                      invisible="move_type not in ('in_invoice', 'in_refund')">

                    <group>
                        <group string="Tax Information">
                            <field name="with_tax"
                                   widget="boolean_toggle"/>
                        </group>
                    </group>

                    <group>
                        <group string="Invoice Fiscal Data">
                            <field name="auth_number"
                                   placeholder="e.g., 79040011007"/>

                            <field name="control_code"
                                   placeholder="e.g., 6A-DC-53-E6-00"/>

                            <field name="invoice_number"
                                   placeholder="e.g., 12345"/>

                            <field name="dui"
                                   placeholder="For imports only"/>
                        </group>
                    </group>

                </page>
            </xpath>

        </field>
    </record>

    <!-- Vista de árbol/lista - agregar columnas opcionales para filtrado -->
    <record id="view_move_tree_bo_fields" model="ir.ui.view">
        <field name="name">account.move.tree.bo.fields</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_invoice_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="auth_number" optional="hide"/>
                <field name="invoice_number" optional="hide"/>
                <field name="control_code" optional="hide"/>
            </xpath>
        </field>
    </record>

</odoo>